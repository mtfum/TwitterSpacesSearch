enum Secret {
    static let twitterConsumerKey = "{{argument.twitterConsumerKey }}"
    static let twitterConsumerSecret = "{{ argument.twitterConsumerSecret }}"
    static let twitterBearerToken = "{{ argument.twitterBearerToken }}"
}

