import XCTest
@testable import TwitterSpacesSearch

final class TwitterSpacesSearchTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TwitterSpacesSearch().text, "Hello, World!")
    }
}
